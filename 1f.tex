\section{јнализ соответстви€ модели кластерообразовани€ с использованием 
весовых множителей многокомпонентному уравнению —молуховского.}

„тобы показать соответствие случайного процесса ур-ю —молуховского рассмотрим функцию 
$$
\phi(l, t) = \sum_{i=1}^{N} w_i[l_i=l]
$$
и предположим, что веса частиц распределены независимо. 

–ассмотрим эволюцию концентрации частиц размера $m$ за врем€ $dt$. ”величение концентрации будет происходить за  счет столкновений
частиц $p_i$ и $p_j$, размер которых удовлетвор€ет условию $l(p_i) + l(p_j) = m$, при этом концентраци€ увеличитс€ на $\min(w(p_i), w(p_j))$.
ћатематическое ожидание прироста концентрации будет составл€ть 
\begin{align*}
I^+ (l, t+dt) &= \frac12 \sum_{i=1}^{N}\sum_{j=1}^N p_{ij} \min(w_i, w_j)[l_i+l_j = l]=\\
&\frac12 \sum_{i = 1}^{N}\sum_{j=1}^{N} w_i w_j K(l_i, l_j) dt [l_i + l_j = l]=
\frac12 \sum_{i = 1}^{N} w_i K(l_i, l - l_i) dt \sum_{j=1}^{N} w_j[l_j = l - l_i]=\\
&\frac12 \sum_{i = 1}^{N} w_i K(l_i, l - l_i) dt \phi(l - l_i, t)[l_i \neq l/2] + 
\frac12 \sum_{i = 1}^{N} w_i K(l/2, l/2) dt (\phi (l/2, t) - w_i) =&\\
&\frac12 \sum_{x = 1}^{l-1} \phi (x, t) \phi(l-x, t) K(l, l-x) dt - 
\frac12 \sum_{i = 1}^{N} w_i^2 K(l/2, l/2) dt[l_i = l/2]
\end{align*}

ћножитель $\frac12$ возникает, потому что мы пробегаем по частицам дважды. ¬о второй строчке - записываем сразу 
мат.ожидание прибавки веса и выносим из суммы $K$ и $w_i$, в силу независимости частиц $i$ и $j$.
¬ конце оказываетс€, что нам недостает частоты столкновени€ внутри сорта $l/2$. ¬ведем дополнительный множитель
дл€ столкновений частиц одного размера:
$$
m(l) = \frac{(\sum_{i=1}^{N}w_i)^2}{(\sum_{i=1}^{N}w_i)^2 - \sum_{i=1}^{N}w_i^2}[l_i = l]
$$

“огда данна€ сумма будет совпадать с первым членом в уравнении —молуховского.
ћатематическое ожидание уменьшени€ концентраций вычисл€етс€ аналогичным образом:
\begin{align*}
&I^- (l, t+dt) = \sum_{i=1}^{N}\sum_{j=1}^{N} w_i w_j K(l_i, l_j) dt [l_i = l][i \neq j] = \\
&\sum_{x=1}^{\max l} \sum_{i=1}^{N} w_i K(l, x) \phi(x, t) dt [l_i = l] [x \neq l] + \frac12 \sum_{i=1}^{N} 2 w_i w_j K(l, l) dt [l_i=l_j = l]\\
&\sum_{x=1}^{\max l} K(l, x) \phi(l, t) \phi(x, t) dt - \sum_{i=1}^{N} w_i^2 k(l, l) dt [l_i=l]
\end{align*}

”читыва€ поправочный множитель $m(l)$ дл€ частиц одинакового размера столкновений получим второй член в уравнении —молуховского. ¬ выводе 
$\frac12$ возникает из-за двойного обхода, а 2 - из-за, того, что вес уменьшаетс€ сразу у двух частиц.

“аким образом,  $\phi(l, t)$ совпадает с решением дискретного уравнени€ —молуховского.




