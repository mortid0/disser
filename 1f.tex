\section{јнализ соответстви€ модели кластерообразовани€ с использованием 
весовых множителей многокомпонентному уравнению —молуховского.}

„тобы показать соответствие случайного процесса ур-ю —молуховского рассмотрим функцию 
$$
\phi(l, t) = \sum_{i=1}^{N} w_i[\vec l_i = \vec l]
$$
и предположим, что веса частиц распределены независимо. 

–ассмотрим эволюцию концентрации частиц размера $\vec m$ за врем€ $\Delta t$. ”величение концентрации будет происходить за  счет столкновений
частиц $p_i$ и $p_j$, размер которых удовлетвор€ет условию $\vec l_i + \vec l_j = \vec m$, при этом концентраци€ увеличитс€ на $\min(w_i, w_j)$.
ћатематическое ожидание прироста концентрации будет составл€ть 
\begin{align*}
I^+ (\vec l, t+dt) &= \frac12 \sum_{i=1}^{N}\sum_{j=1}^N p_{ij} \min(w_i, w_j)[\vec l_i+\vec l_j = \vec l]=\\
&\frac12 \sum_{i = 1}^{N}\sum_{j=1}^{N} w_i w_j K(\vec l_i, \vec l_j) dt [\vec l_i + \vec l_j = l]=
\frac12 \sum_{i = 1}^{N} w_i K(\vec l_i, \vec l - \vec l_i) dt \sum_{j=1}^{N} w_j[\vec l_j = \vec l - \vec l_i]=\\
&\frac12 \sum_{i = 1}^{N} w_i K(\vec l_i, \vec l - \vec l_i) dt \phi(\vec l - \vec l_i, t)[\vec l_i \neq \vec l/2] + 
\frac12 \sum_{i = 1}^{N} w_i K(\vec l/2, \vec l/2) dt (\phi (\vec l/2, t) - w_i) =&\\
&\frac12 \sum_{x} \phi (\vec x, t) \phi(\vec l-\vec x, t) K(\vec l, \vec l-\vec x) dt - 
\frac12 \sum_{i = 1}^{N} w_i^2 K(\vec l/2, \vec l/2) dt[\vec l_i = \vec l/2]
\end{align*}

ћножитель $\frac12$ возникает, потому что мы проходим по всем парам частиц дважды. ¬о второй строчке - записываем сразу 
мат.ожидание прибавки веса и выносим из суммы $K$ и $w_i$, в силу независимости частиц $i$ и $j$.
¬ конце оказываетс€, что нам недостает частоты столкновени€ внутри сорта $\vec \vec l/2$. ¬ведем дополнительный множитель
дл€ столкновений частиц одного размера:
$$
m(l) = \frac{(\sum_{i=1}^{N}w_i)^2}{(\sum_{i=1}^{N}w_i)^2 - \sum_{i=1}^{N}w_i^2}[\vec l_i = \vec l]
$$

“огда данна€ сумма будет совпадать с первым членом в уравнении —молуховского.
ћатематическое ожидание уменьшени€ концентраций вычисл€етс€ аналогичным образом:
\begin{align*}
&I^- (\vec l, t+dt) = \sum_{i=1}^{N}\sum_{j=1}^{N} w_i w_j K(\vec l_i, \vec l_j) dt [\vec l_i = \vec l][i \neq j] = \\
&\sum_{x=1}^{\max \vec l} \sum_{i=1}^{N} w_i K(\vec l, \vec x) \phi(\vec x, t) dt [\vec l_i = \vec l] [\vec x \neq \vec l] + \frac12 \sum_{i=1}^{N} 2 w_i w_j K(\vec l, \vec l) dt [l_i=l_j = l]\\
&\sum_{x=1}^{\max \vec l} K(l, x) \phi(\vec l, t) \phi(\vec x, t) dt - \sum_{i=1}^{N} w_i^2 k(\vec l, \vec l) dt [l_i=l]
\end{align*}

”читыва€ поправочный множитель $m(\vec l)$ дл€ частиц одинакового размера столкновений получим второй член в уравнении —молуховского. ¬ выводе 
$\frac12$ возникает из-за двойного обхода, а 2 - из-за, того, что вес уменьшаетс€ сразу у двух частиц.

“аким образом,  $\phi(\vec l, t)$ совпадает с решением многокомпонентного уравнени€ —молуховского.
